// (tabs)/index.tsx — "Tab One" / Dashboard tab
// This simply renders your existing Dashboard component from (app)/Dashboard.tsx
// keeping the tab navigator clean and the business logic in one place.

import Dashboard from '../../app/(app)/Dashboard';

export default Dashboard;